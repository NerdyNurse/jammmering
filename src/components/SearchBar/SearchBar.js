import React from 'react'

export default function SearchBar({userInput}) {

    return(
        <div>
            
            <input type='text' />
            <button>search</button>
        </div>
    )
}