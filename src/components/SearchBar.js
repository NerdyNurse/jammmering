import React from 'react'

export default function SearchBar() {

    return(
        <div>
            <h1>Search Bar</h1>
            <input type='text' />
            <button>search</button>
        </div>
    )
}